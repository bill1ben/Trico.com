<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand" href="{{ path('home') }}">Trico</a>
    <button class="navbar-toggler collapsed" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="navbar-collapse collapse" id="navbarColor01" style="">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="{{ path("home") }}">Accueil <span class="sr-only">(current)</span></a>
            </li>



        </ul>
        <ul class="navbar-nav ml-auto">
            {% if app.user %}

                <li class="nav-item dropdown">
                    <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" id="accounteDropDownLink">
                        <img src="{{ app.user.picture }}" class="avatar avatar-mini" alt="avatar de  {{ app.user.fullName }} ">  {{ app.user.fullName }}
                    </a>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accounteDropDownLink">
                        <a href="{{ path('product_new') }}" class="dropdown-item">ajouter un t-shirt</a>
                        <div class="dropdown-divider">

                        </div>
                           <a href="{{ path('accounte_myIndex') }}" class="dropdown-item">mon compt</a>
                           <a href="{{ path('accounte_profile') }}" class="dropdown-item">modifier mon profile</a>
                        <div class="dropdown-divider">

                        </div>
                           <a href="{{ path('accounte_logout') }}" class="dropdown-item">deconnexion</a>
                    </div>
                </li>


            {% else %}
                <li class="nav-item ">
                    <a href="{{ path('accounte_register') }}" class="nav-link">
                      Inscription
                    </a>
                </li>
                <li class="nav-item ">
                    <a href="{{ path('accounte_login') }}" class="nav-link">
                            Connexion
                    </a>
                </li>
            {% endif %}

        </ul>
    </div>
</nav>
